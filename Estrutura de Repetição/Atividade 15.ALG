algoritmo "sem_nome"
var

   c, t, n: inteiro

inicio

   escreva("Informe um número que deseja ver a tabuada: ")
   leia(n)
   c <- 1
   para c de 1 ate 10 faca
      t <- n * c
      escreval(n, " * ", c, " = ", t)
   fimpara

fimalgoritmo
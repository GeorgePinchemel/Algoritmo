algoritmo "media100numeros"
var

   n, c, m: inteiro

inicio

   n <- 0
   c <- 1
   Para c de 1 ate 100 faca
      n <- n + c
   fimpara
   m <- n DIV 100
   escreva("A média aritmética é igual a", m)

fimalgoritmo